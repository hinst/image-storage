import * as path from "path"
export const WebPath = "/image-storage"
export const AppDir = path.normalize(__dirname + "/..")
export const AppBinDir = path.normalize(__dirname)
export const Port = 3000
export const AppWwwBinDir = AppDir + "/www-bin"